# Reviewed host capability facts for fixture-postgres-api/staging.
# Host inventory target: fixtures/app-suite/host-inventory.yml
version: 1
hosts:
  - id: fixture-edge-a
    capabilities:
      backups: true
      caddy: true
      docker: true
      edge: true
      postgres: true
