# Operator Handoff Note

Status: private material removed from public tree

This file previously contained a private operator handoff. Public Ophelia docs
now keep only the reusable contract:

- use installed `ship` commands
- prefer fixture-backed validation before live evidence
- keep secrets and runtime state outside Git
- require plans, confirmation tokens, receipts, and rollback context for
  mutations

Private deployment handoffs should live outside the public repository.
